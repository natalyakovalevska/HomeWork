package home_work_1;

import java.util.Scanner;

public class Task5 {

    public static void main(String[] args) {
        Scanner console = new Scanner(System.in);
        Object bolean;
        boolean getBoolean;
        boolean weekday = getBoolean(console);
        boolean vacation = getBoolean(console);
        private static boolean sleepIn;
        if (sleepIn(weekday)) {
            System.out.println("Пора идти на работу");
            if (sleepIn(vacation)) {
                System.out.println("Можем спать дальше");
            }

        }

        private static boolean getBoolean (Scanner console) {
        }

        ( boolean weekday) {
        }
    }

    private static boolean getBoolean(Scanner console) {
    }

    private static boolean sleepIn(boolean weekday) {
    }
}